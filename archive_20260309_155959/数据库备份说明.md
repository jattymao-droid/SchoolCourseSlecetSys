# 数据库备份完成报告

## 备份概要

✅ 数据库已成功导出到 `sql` 文件夹

### 备份文件信息
- **文件路径**: `sql/ry_vue_full_backup_20260308_210240.sql`
- **文件大小**: 0.72 MB (752,086 字节)
- **导出时间**: 2026年3月8日 21:02:40
- **数据库名**: ry_vue
- **数据库类型**: PostgreSQL 17.5

### 备份内容统计
- **数据表数量**: 39 个表
- **数据记录**: 2,512 条 INSERT 语句
- **备份类型**: 完整备份（结构 + 数据）

## 主要数据表

### 课程选课系统 (6个表)
- `cou_course` - 课程信息
- `cou_class_quota` - 班级课程容量
- `stu_selection` - 学生选课记录
- `stu_student_info` - 学生基本信息
- `stu_evaluation` - 课程评价
- `eva_question` - 评价问题

### 系统管理 (8个表)
- `sys_user` - 用户账号
- `sys_role` - 角色权限
- `sys_menu` - 菜单配置
- `sys_dept` - 部门组织
- `sys_config` - 系统配置
- `sys_dict_type` / `sys_dict_data` - 数据字典
- `sys_post` - 岗位信息

### 学校基础数据 (3个表)
- `sys_grade` - 年级信息
- `sys_class` - 班级信息
- `sys_semester` - 学期信息

### 系统日志 (3个表)
- `sys_oper_log` - 操作日志
- `sys_logininfor` - 登录日志
- `sys_job_log` - 定时任务日志

### 定时任务 (11个表)
- `qrtz_*` - Quartz 定时任务框架表

### 代码生成 (2个表)
- `gen_table` - 代码生成配置
- `gen_table_column` - 字段配置

## 可用脚本

### 1. 导出脚本 - `export-db.ps1`
自动导出数据库到 sql 文件夹

```powershell
# 使用方法
./export-db.ps1
```

**功能特点**:
- 自动读取数据库配置
- 生成带时间戳的备份文件
- 显示导出进度和统计信息
- 使用 INSERT 格式（更兼容）

### 2. 恢复脚本 - `restore-database.ps1`
从备份文件恢复数据库

```powershell
# 使用方法
./restore-database.ps1
```

**功能特点**:
- 列出所有可用备份文件
- 交互式选择备份文件
- 指定目标数据库名称
- 安全确认机制

## 如何使用备份

### 快速恢复（覆盖现有数据库）
```powershell
# 方法1: 使用恢复脚本（推荐）
./restore-database.ps1

# 方法2: 使用 psql 命令
$env:PGPASSWORD = "mm5621528"
psql -h localhost -p 5432 -U postgres -d ry_vue -f sql/ry_vue_full_backup_20260308_210240.sql
Remove-Item Env:\PGPASSWORD
```

### 恢复到新数据库
```powershell
# 1. 创建新数据库
createdb -U postgres ry_vue_backup

# 2. 恢复数据
$env:PGPASSWORD = "mm5621528"
psql -h localhost -p 5432 -U postgres -d ry_vue_backup -f sql/ry_vue_full_backup_20260308_210240.sql
Remove-Item Env:\PGPASSWORD
```

## 备份策略建议

### 开发环境
- **频率**: 每周一次
- **保留**: 最近 4 个版本
- **时机**: 重大功能开发前

### 测试环境
- **频率**: 每天一次
- **保留**: 最近 7 天
- **时机**: 每日下班前

### 生产环境
- **频率**: 每天多次
- **保留**: 30 天内所有版本
- **时机**: 
  - 每天凌晨 2:00（自动）
  - 系统升级前（手动）
  - 重要操作前（手动）

## 自动化备份

### 使用 Windows 任务计划程序

1. 打开"任务计划程序"
2. 创建基本任务
3. 设置触发器（每天/每周）
4. 操作：启动程序
   - 程序：`powershell.exe`
   - 参数：`-File "D:\AI\StuCourceSys\SchoolCourseSlecetSys\export-db.ps1"`
5. 完成设置

### 备份文件管理

建议定期清理旧备份文件：

```powershell
# 删除 30 天前的备份文件
Get-ChildItem -Path "sql" -Filter "*.sql" | 
    Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-30) } | 
    Remove-Item
```

## 安全注意事项

1. **密码保护**: 备份文件不包含密码，但包含所有业务数据
2. **访问控制**: 限制备份文件的访问权限
3. **异地备份**: 定期将备份文件复制到其他位置
4. **加密存储**: 对于敏感数据，考虑加密备份文件
5. **定期测试**: 定期测试备份恢复流程

## 故障恢复流程

### 数据丢失恢复
1. 停止应用服务
2. 使用最新备份恢复数据库
3. 验证数据完整性
4. 重启应用服务

### 数据损坏恢复
1. 评估损坏范围
2. 选择合适的备份点
3. 恢复到临时数据库
4. 导出需要的数据
5. 修复主数据库

## 验证备份完整性

```powershell
# 检查备份文件
Get-Content sql/ry_vue_full_backup_20260308_210240.sql | Select-String "CREATE TABLE" | Measure-Object

# 预期结果: 39 个表
```

## 相关文档

- `sql/README.md` - 详细的备份和恢复说明
- `export-db.ps1` - 导出脚本源码
- `restore-database.ps1` - 恢复脚本源码

## 技术支持

如遇到问题，请检查：
1. PostgreSQL 是否正常运行
2. 数据库连接配置是否正确
3. 用户权限是否足够
4. 磁盘空间是否充足

---

**备份完成时间**: 2026-03-08 21:02:40  
**备份执行者**: 系统管理员  
**备份状态**: ✅ 成功
