# 服务器部署包打包完成 ✅

## 打包信息

- **部署包文件**: `course-selection-server_20260309_161231.zip`
- **文件大小**: 78.37 MB
- **打包时间**: 2026-03-09 16:12:31
- **位置**: `D:\AI\StuCourceSys\SchoolCourseSlecetSys\course-selection-server_20260309_161231.zip`

## 包含内容

### 1. 后端应用
- **文件**: `backend/ruoyi-admin.jar`
- **大小**: 83.28 MB
- **配置**: 使用 `druid,prod` profile

### 2. 前端应用
- **目录**: `frontend/dist/`
- **文件数**: 331 个
- **包含**: HTML、CSS、JS、图片等静态资源

### 3. 数据库备份
- **文件**: `database/database_backup.dump`
- **大小**: 0.13 MB
- **格式**: PostgreSQL 自定义格式
- **来源**: 本地 `ry_vue` 数据库

### 4. 部署脚本
- **文件**: `deploy.sh`
- **功能**: 一键自动部署
- **特性**:
  - 自动检测 Docker 容器
  - 自动删除旧数据库并重新导入
  - 自动配置 Nginx
  - 自动创建管理脚本
  - 自动启动后端服务

### 5. 文档
- **README.md**: 详细部署说明
- **部署包说明.txt**: 快速参考

## 服务器环境要求

✅ CentOS 7
✅ 宝塔面板
✅ Docker (PostgreSQL + Redis)
✅ Java 1.8+
✅ Nginx

## 部署步骤（3步）

### 1. 上传部署包

```bash
# 使用 SCP 上传
scp course-selection-server_20260309_161231.zip root@服务器IP:/root/

# 或使用宝塔面板文件管理上传
```

### 2. 解压

```bash
cd /root
unzip course-selection-server_20260309_161231.zip
cd deploy-server
```

### 3. 执行部署

```bash
chmod +x deploy.sh
./deploy.sh
```

## 部署脚本功能

脚本会自动完成以下操作：

1. ✓ 检查系统环境
   - Java 版本
   - Docker 状态
   - PostgreSQL 容器
   - Redis 容器
   - Nginx 状态

2. ✓ 创建目录结构
   - `/www/wwwroot/course-selection/backend/`
   - `/www/wwwroot/course-selection/frontend/`
   - `/www/wwwroot/course-selection/logs/`
   - `/www/wwwroot/course-selection/database/`

3. ✓ 部署后端应用
   - 停止旧服务
   - 复制 JAR 文件
   - 创建启动脚本

4. ✓ 部署前端应用
   - 复制静态文件到 Nginx 目录

5. ✓ 导入数据库
   - **自动删除旧的 `school-course` 数据库**
   - 创建新数据库
   - 导入数据

6. ✓ 配置 Nginx
   - 创建配置文件
   - 配置反向代理
   - 重载 Nginx

7. ✓ 创建管理脚本
   - `start.sh` - 启动后端
   - `stop.sh` - 停止后端
   - `restart.sh` - 重启后端

8. ✓ 启动后端服务
   - 自动启动应用
   - 验证启动状态

## 部署后访问

- **前端**: http://服务器IP
- **后端API**: http://服务器IP:8080

## 默认账号

- **管理员**: admin / admin123
- **学生**: 学号 / 123456
- **教师**: 工号 / 123456

## 管理命令

```bash
# 启动后端
/www/wwwroot/course-selection/backend/start.sh

# 停止后端
/www/wwwroot/course-selection/backend/stop.sh

# 重启后端
/www/wwwroot/course-selection/backend/restart.sh

# 查看日志
tail -f /www/wwwroot/course-selection/logs/backend.log
```

## 数据库配置

- **数据库名**: school-course（自动创建）
- **用户名**: postgres
- **密码**: mm5621528
- **端口**: 5432
- **连接**: Docker 容器

## Redis 配置

- **主机**: localhost
- **端口**: 6379
- **密码**: 无
- **连接**: Docker 容器

## 重要提示

### ⚠️ 数据库会被重新创建

部署脚本会：
1. 断开所有连接到 `school-course` 数据库的会话
2. 删除旧的 `school-course` 数据库
3. 创建新的 `school-course` 数据库
4. 导入备份数据

**如果服务器上已有重要数据，请先备份！**

### ✅ 自动化特性

- 自动检测 Docker 容器名称
- 自动配置 Nginx 反向代理
- 自动创建所有必要目录
- 自动启动后端服务
- 自动验证部署状态

## 故障排查

### 1. 后端启动失败

```bash
# 查看日志
tail -f /www/wwwroot/course-selection/logs/backend.log

# 检查 PostgreSQL
docker ps | grep postgres

# 检查 Redis
docker ps | grep redis
```

### 2. 前端无法访问

```bash
# 检查 Nginx
systemctl status nginx
nginx -t

# 检查后端
ps aux | grep ruoyi-admin.jar
```

### 3. 数据库导入失败

```bash
# 手动导入
docker exec -i <PostgreSQL容器名> pg_restore -U postgres -d school-course -v < /www/wwwroot/course-selection/database/database_backup.dump
```

## 详细文档

部署包中包含完整的部署文档：
- `README.md` - 详细部署说明
- `部署包说明.txt` - 快速参考

本地也有详细说明：
- `服务器部署说明.md` - 完整的部署和管理指南

## 下一步

1. ✅ 上传 `course-selection-server_20260309_161231.zip` 到服务器
2. ✅ 解压并执行 `deploy.sh`
3. ✅ 访问系统并测试功能
4. ✅ 修改默认密码
5. ✅ 配置 SSL 证书（可选）

## 完成！

部署包已准备就绪，可以上传到服务器进行部署了。

如有任何问题，请查看部署包中的 README.md 或本地的 `服务器部署说明.md`。
