# 生产环境配置
# 部署时将此文件放在 JAR 同目录，或通过 --spring.config.additional-location 指定
spring:
  redis:
    host: localhost
    port: 6379
    password:
  datasource:
    druid:
      master:
        url: jdbc:postgresql://localhost:5432/ry_vue?useUnicode=true&characterEncoding=utf8
        username: postgres
        password: mm5621528

ruoyi:
  # Linux 文件上传路径
  profile: /home/ruoyi/uploadPath

referer:
  allowed-domains: localhost,127.0.0.1,xk.xmls.vip,www.xk.xmls.vip
