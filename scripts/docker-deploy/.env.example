# Docker 部署环境变量（可选）
# 复制为 .env 并修改。当 host.docker.internal 在 Linux 上不可用时使用。

# PostgreSQL（宿主机宝塔安装）
POSTGRES_HOST=172.17.0.1
POSTGRES_PORT=5432
POSTGRES_DB=ry_vue
POSTGRES_USER=postgres
PG_PASSWORD=mm5621528

# Redis（宿主机宝塔安装）
REDIS_HOST=172.17.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# 前端端口（默认 9090）
# FRONTEND_PORT=9090
