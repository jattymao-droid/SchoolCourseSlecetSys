# ✅ 数据库已重新导出

## 问题原因

后端报错：`ERROR: relation "sys_menu" does not exist`

**根本原因**：服务器数据库是空的，没有任何表！

之前的数据库备份文件可能不完整或者没有正确导入到服务器。

---

## 已完成的工作

### 1. 从本地数据库重新导出完整 SQL

✅ 已成功导出完整数据库：
- **文件名**：`ry_vue_complete_20260310_000408.sql`
- **文件大小**：717 KB
- **表数量**：39 个
- **包含数据**：✅ 是
- **包含 admin 用户**：✅ 是（user_id=1, 密码: admin123）

### 2. 复制到部署包

✅ 已复制到部署包：
- **位置**：`SchoolCourseSlecetSys/deploy/sql/complete-database.sql`
- **大小**：717 KB

### 3. 创建导入脚本和文档

✅ 已创建：
- `deploy/scripts/import-database.sh` - 自动导入脚本
- `🔧 导入完整数据库.md` - 详细导入指南

---

## 📁 文件位置

### 本地文件
```
SchoolCourseSlecetSys/
├── sql/
│   └── ry_vue_complete_20260310_000408.sql  (最新完整备份)
└── deploy/
    ├── sql/
    │   └── complete-database.sql             (部署包中的副本)
    └── scripts/
        └── import-database.sh                (自动导入脚本)
```

### 需要上传到服务器
- `deploy/sql/complete-database.sql` → `/www/wwwroot/xk.xmls.vip/sql/complete-database.sql`
- `deploy/scripts/import-database.sh` → `/www/wwwroot/xk.xmls.vip/scripts/import-database.sh`

---

## 🚀 现在需要做的事

### 步骤 1：上传 SQL 文件到服务器

使用 FTP 或宝塔面板上传：
- **本地文件**：`SchoolCourseSlecetSys/deploy/sql/complete-database.sql`
- **服务器路径**：`/www/wwwroot/xk.xmls.vip/sql/complete-database.sql`

### 步骤 2：导入数据库

#### 方法 A：使用自动脚本（推荐）

```bash
# 1. 上传导入脚本（如果还没上传）
# 本地: SchoolCourseSlecetSys/deploy/scripts/import-database.sh
# 服务器: /www/wwwroot/xk.xmls.vip/scripts/import-database.sh

# 2. 执行导入脚本
cd /www/wwwroot/xk.xmls.vip/scripts
chmod +x import-database.sh
./import-database.sh
```

#### 方法 B：手动执行命令

```bash
# 1. 删除并重建数据库
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -c "DROP DATABASE IF EXISTS \"school-course\";"
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -c "CREATE DATABASE \"school-course\" WITH ENCODING='UTF8';"

# 2. 导入数据库
docker exec -i postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course < /www/wwwroot/xk.xmls.vip/sql/complete-database.sql

# 3. 验证导入
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public';"
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "SELECT user_id, user_name, nick_name FROM sys_user WHERE user_name = 'admin';"
```

### 步骤 3：重启后端服务

```bash
cd /www/wwwroot/xk.xmls.vip/backend
./restart.sh

# 查看启动日志
tail -f backend.log
```

**成功标志**：日志中出现 `Started RuoYiApplication`，没有数据库错误。

### 步骤 4：测试登录

访问：`http://xk.xmls.vip`

使用以下账号登录：
- **用户名**：`admin`
- **密码**：`admin123`

---

## 📊 数据库内容

### 包含的表（39 个）

#### 课程选课系统（6 个表）
- `cou_course` - 课程表
- `cou_class_quota` - 课程班级容量表
- `stu_selection` - 学生选课表
- `stu_student_info` - 学生信息表
- `stu_evaluation` - 课程评价表
- `eva_question` - 评价问题表

#### 系统管理（15 个表）
- `sys_user` - 用户表
- `sys_role` - 角色表
- `sys_menu` - 菜单表
- `sys_dept` - 部门表
- `sys_config` - 系统配置表
- `sys_dict_type` - 字典类型表
- `sys_dict_data` - 字典数据表
- `sys_post` - 岗位表
- `sys_notice` - 通知公告表
- `sys_role_dept` - 角色部门关联表
- `sys_role_menu` - 角色菜单关联表
- `sys_user_post` - 用户岗位关联表
- `sys_user_role` - 用户角色关联表
- `sys_oper_log` - 操作日志表
- `sys_logininfor` - 登录日志表

#### 学校基础数据（3 个表）
- `sys_grade` - 年级表
- `sys_class` - 班级表
- `sys_semester` - 学期表

#### 定时任务（Quartz，11 个表）
- `sys_job` - 定时任务表
- `sys_job_log` - 定时任务日志表
- `qrtz_*` - Quartz 调度器表（9 个）

#### 代码生成（2 个表）
- `gen_table` - 代码生成业务表
- `gen_table_column` - 代码生成字段表

### 包含的数据

✅ **系统管理员**
- 用户名：admin
- 密码：admin123（BCrypt 加密）
- 角色：超级管理员
- 权限：所有权限

✅ **系统基础数据**
- 角色、菜单、权限配置
- 字典数据
- 系统配置参数

✅ **测试数据**（如果有）
- 测试用户
- 测试课程
- 测试学生

---

## ✅ 验证导入成功

### 1. 检查表数量

```bash
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public';"
```

**预期输出**：`39`

### 2. 检查 admin 用户

```bash
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "SELECT user_id, user_name, nick_name, status FROM sys_user WHERE user_name = 'admin';"
```

**预期输出**：
```
 user_id | user_name | nick_name | status
---------+-----------+-----------+--------
       1 | admin     | 管理员    | 0
```

### 3. 检查 sys_menu 表

```bash
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "SELECT COUNT(*) FROM sys_menu;"
```

**预期输出**：应该有几十条菜单记录

### 4. 测试后端连接

```bash
# 重启后端
cd /www/wwwroot/xk.xmls.vip/backend
./restart.sh

# 查看日志（应该没有数据库错误）
tail -f backend.log
```

### 5. 测试登录

浏览器访问：`http://xk.xmls.vip`

登录：`admin` / `admin123`

**预期结果**：
- ✅ 成功登录
- ✅ 看到系统首页
- ✅ 左侧菜单显示正常
- ✅ 可以访问各个功能模块

---

## 📋 完整检查清单

```
[ ] 1. SQL 文件已上传到服务器（717 KB）
[ ] 2. 数据库已删除（DROP DATABASE）
[ ] 3. 数据库已重新创建（CREATE DATABASE）
[ ] 4. SQL 文件已成功导入（无错误）
[ ] 5. 表数量正确（39 个表）
[ ] 6. admin 用户存在（user_id=1）
[ ] 7. sys_menu 表有数据
[ ] 8. 后端服务已重启
[ ] 9. 后端启动成功（无数据库错误）
[ ] 10. 可以成功登录系统
[ ] 11. 菜单显示正常
[ ] 12. 功能模块可以正常访问
```

---

## 🐛 如果还有问题

### 问题 1：导入时提示"文件不存在"

**解决**：确认 SQL 文件路径正确
```bash
ls -lh /www/wwwroot/xk.xmls.vip/sql/complete-database.sql
```

### 问题 2：导入后表数量不对

**解决**：检查导入过程是否有错误
```bash
# 重新导入，查看详细输出
docker exec -i postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course < /www/wwwroot/xk.xmls.vip/sql/complete-database.sql 2>&1 | tee import.log
```

### 问题 3：后端仍然报错"relation does not exist"

**检查**：
```bash
# 1. 确认数据库名称
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -l | grep school-course

# 2. 确认表已导入
docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "\dt"

# 3. 检查后端配置
# 应该是：jdbc:postgresql://localhost:5432/school-course
```

---

## 📞 需要帮助？

如果导入过程中遇到问题，请提供：

1. **导入命令的输出**（完整的错误信息）
2. **表列表**：
   ```bash
   docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -d school-course -c "\dt"
   ```
3. **后端日志**：
   ```bash
   tail -100 /www/wwwroot/xk.xmls.vip/backend/backend.log
   ```
4. **数据库列表**：
   ```bash
   docker exec postgresql_d6wx-postgresql_D6Wx-1 psql -U postgres -l
   ```

---

## 🎉 导入成功后

系统应该完全正常运行：
- ✅ 前端可以访问
- ✅ 可以登录（admin/admin123）
- ✅ 看到完整的菜单
- ✅ 所有功能模块正常
- ✅ 可以管理用户、角色、权限
- ✅ 可以管理课程、学生、选课
- ✅ 可以查看系统日志

---

**文件信息**：
- 导出时间：2026/3/10 00:04:08
- SQL 文件：`deploy/sql/complete-database.sql`
- 文件大小：717 KB
- 表数量：39 个
- 包含数据：✅ 是
- 包含 admin 用户：✅ 是
- 导入脚本：`deploy/scripts/import-database.sh`
- 导入指南：`🔧 导入完整数据库.md`
