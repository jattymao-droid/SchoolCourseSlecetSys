# 课程导入事务错误修复 - 部署包已创建

## 部署包信息

- **文件名**: `course-fix-transaction_20260309_205702.zip`
- **大小**: 74.28 MB
- **位置**: `SchoolCourseSlecetSys/course-fix-transaction_20260309_205702.zip`
- **创建时间**: 2026-03-09 20:57

## 修复内容

修复了云服务器上导入课程时的PostgreSQL事务错误：
```
ERROR: current transaction is aborted, commands ignored until end of transaction block
```

### 代码修改

修改了 `CourseServiceImpl.java` 中的事务处理：

1. **findGradeByName** - 添加 `@Transactional(propagation = Propagation.NOT_SUPPORTED, readOnly = true)`
2. **findSemesterByName** - 添加 `@Transactional(propagation = Propagation.NOT_SUPPORTED, readOnly = true)`
3. **findClassByGradeAndShortName** - 添加 `@Transactional(propagation = Propagation.NOT_SUPPORTED, readOnly = true)`
4. **findUserByNickName** - 添加 `@Transactional(propagation = Propagation.NOT_SUPPORTED, readOnly = true)`
5. **findOrCreateTeacherUser** - 拆分为两个方法：
   - `findTeacherUser` - 只读查询（NOT_SUPPORTED）
   - `createTeacherUser` - 创建用户（REQUIRES_NEW）

### 修复原理

- 查询方法设置为非事务执行（`Propagation.NOT_SUPPORTED`）
- 避免在主事务中断后，查询SQL被PostgreSQL拒绝执行
- 创建用户的操作在新事务中执行（`Propagation.REQUIRES_NEW`）

## 部署步骤

### 1. 上传部署包到服务器

```bash
scp course-fix-transaction_20260309_205702.zip root@your-server:/tmp/
```

### 2. SSH登录服务器

```bash
ssh root@your-server
```

### 3. 解压部署包

```bash
cd /tmp
unzip course-fix-transaction_20260309_205702.zip
cd deploy-course-fix-transaction_20260309_205702
```

### 4. 执行部署脚本

```bash
chmod +x deploy.sh
./deploy.sh
```

部署脚本会自动：
- 备份当前JAR文件
- 停止后端服务
- 部署新JAR文件
- 启动后端服务

### 5. 验证部署

```bash
# 查看日志
tail -f /www/wwwroot/course-selection/logs/backend.log

# 检查服务状态
ps aux | grep ruoyi-admin.jar

# 检查端口
netstat -tlnp | grep 8080
```

### 6. 测试课程导入

1. 登录系统: https://xk.xmls.vip
2. 进入课程管理
3. 尝试导入课程
4. 验证是否还有事务错误

## 包含文件

```
deploy-course-fix-transaction_20260309_205702/
├── backend/
│   └── ruoyi-admin.jar (83.28 MB)
├── docs/
│   ├── 课程导入修复-部署说明.md
│   └── 课程导入事务错误修复方案.md
├── deploy.sh
└── README.txt
```

## 回滚方案

如果部署后出现问题，可以快速回滚：

```bash
# 停止后端
pkill -f ruoyi-admin.jar

# 恢复备份（使用最新的备份文件）
cp /www/wwwroot/course-selection/backups/ruoyi-admin.jar.backup_YYYYMMDD_HHMMSS /www/wwwroot/course-selection/backend/ruoyi-admin.jar

# 启动后端
cd /www/wwwroot/course-selection/backend
nohup java -jar ruoyi-admin.jar --spring.profiles.active=druid,prod > /www/wwwroot/course-selection/logs/backend.log 2>&1 &
```

## 常见问题

### Q1: 部署后仍然报错

**解决方法**:
```bash
# 1. 验证JAR文件大小
ls -lh /www/wwwroot/course-selection/backend/ruoyi-admin.jar

# 2. 强制停止并重启
pkill -9 -f ruoyi-admin.jar
cd /www/wwwroot/course-selection/backend
nohup java -jar ruoyi-admin.jar --spring.profiles.active=druid,prod > /www/wwwroot/course-selection/logs/backend.log 2>&1 &

# 3. 清除浏览器缓存，重新登录
```

### Q2: 服务启动失败

**解决方法**:
```bash
# 查看详细日志
tail -n 200 /www/wwwroot/course-selection/logs/backend.log

# 检查端口占用
netstat -tlnp | grep 8080

# 检查Java进程
ps aux | grep java
```

### Q3: 数据库连接失败

**解决方法**:
```bash
# 检查PostgreSQL容器
docker ps | grep postgresql

# 检查数据库连接
docker exec postgresql_npxc-postgresql_NpXc-1 psql -U postgres -d school-course -c "SELECT 1"
```

## 技术说明

### 事务传播行为

- **NOT_SUPPORTED**: 以非事务方式执行，如果当前存在事务，则挂起当前事务
- **REQUIRES_NEW**: 创建一个新事务，如果当前存在事务，则挂起当前事务

### 为什么这样修复有效？

1. **查询方法不参与事务**: 将查询方法设置为 `NOT_SUPPORTED`，使其在事务外执行
2. **避免事务中断**: 即使主事务中有错误，查询方法仍然可以正常执行
3. **创建操作独立**: 创建用户的操作在新事务中执行，不影响主事务

## 验证清单

部署完成后，请验证以下项目：

- [ ] 后端服务正常启动
- [ ] 可以正常登录系统
- [ ] 课程列表可以正常显示
- [ ] 可以正常导入课程（无事务错误）
- [ ] 错误数据的错误提示正常显示
- [ ] 其他功能（学生管理、选课等）正常工作

## 相关文件

- **部署包**: `course-fix-transaction_20260309_205702.zip`
- **修复方案**: `课程导入事务错误修复方案.md`
- **部署说明**: `课程导入修复-部署说明.md`
- **源代码**: `ruoyi-system/src/main/java/com/ruoyi/system/service/impl/CourseServiceImpl.java`

## 版本信息

- **修复版本**: v1.1
- **创建日期**: 2026-03-09 20:57
- **JAR文件大小**: 83.28 MB
- **JAR编译时间**: 2026-03-09 20:51

## 下一步

1. 上传部署包到服务器
2. 执行部署脚本
3. 测试课程导入功能
4. 如有问题，查看日志或回滚

---

**注意**: 部署前请确保已备份当前运行的JAR文件。部署脚本会自动创建备份，但手动备份更安全。
